import { SerchFilterPipe } from './serch-filter.pipe';

describe('SerchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
